// config.js — Mini Project 2
// Replaces data.js. No static employee array. No hardcoded credentials.
// Only the API base URL constant lives here.
// storageService.js reads this. No other file should reference API_BASE_URL directly.

const API_BASE_URL = 'http://localhost:5000/api';
